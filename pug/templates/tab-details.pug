ion-view(view-title='DETAILS')
  ion-content.has-header.scroll-tab
    .row(ng-repeat='(key,value) in item')
      .col.details
        span {{key}}
        |  : 
        span.pull-right {{value}} 
      span
        a.button.button-icon.icon.ion-ios-cloud-download(ng-if="key == 'FileName'", ng-click='download($event,$index)')
